import 'package:flutter/material.dart';

class AppColors {
  // ── Primary palette ──
  static const primary = Color(0xFF2563EB);
  static const cta = Color(0xFFF97316);
  static const accent = cta;

  // ── Backgrounds ──
  static const background = Color(0xFFFFFFFF);
  static const surface = Color(0xFFF8FAFC);
  static const surfaceAlt = surface;
  static const searchBg = surface;

  // ── Text ──
  static const textPrimary = Color(0xFF111827);
  static const textSecondary = Color(0xFF6B7280);
  static const textDisabled = Color(0xFF9CA3AF);
  static const textHint = textDisabled;
  static const textOnPrimary = Color(0xFFFFFFFF);

  // ── Borders & dividers ──
  static const border = Color(0xFFE5E7EB);
  static const divider = border;
  static const cardBorder = border;

  // ── Semantic ──
  static const success = Color(0xFF10B981);
  static const error = Color(0xFFEF4444);
  static const pending = Color(0xFFF59E0B);
  static const warning = pending;
  static const info = Color(0xFF3B82F6);

  // ── Buttons ──
  static const buttonPrimaryBg = Color(0xFF2563EB);
  static const buttonPrimaryText = Color(0xFFFFFFFF);
  static const buttonBuyBg = Color(0xFFF97316);
  static const buttonBuyText = Color(0xFFFFFFFF);
  static const buttonSuccessBg = Color(0xFF10B981);
  static const buttonSuccessText = Color(0xFFFFFFFF);

  // ── Navigation bar ──
  static const navbarBg = Color(0xFFFFFFFF);
  static const navbarBorder = Color(0xFFE5E7EB);
  static const navbarIconActive = Color(0xFF2563EB);
  static const navbarIconInactive = Color(0xFF9CA3AF);

  // ── Header / App bar ──
  static const headerBg = Color(0xFFFFFFFF);
  static const headerTitle = Color(0xFF111827);
  static const headerIcons = Color(0xFF374151);

  // ── Delivery & Maps ──
  static const driverMarker = Color(0xFF2563EB);
  static const customerMarker = Color(0xFFF97316);
  static const deliveredMarker = Color(0xFF10B981);
  static const routeLine = Color(0xFF2563EB);
  static const routeCompleted = Color(0xFF10B981);

  // ── Specific elements ──
  static const iconBg = Color(0xFFF0F0F0);
  static const starFilled = Color(0xFFF59E0B);
  static const ratingBadge = Color(0xFF10B981);

  // ── Shadows ──
  static const cardShadow = Color(0x08000000);
  static const cardShadowMedium = Color(0x12000000);
}
