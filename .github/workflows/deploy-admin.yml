name: Deploy Admin Dashboard to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'lib/web/admin/**'
      - 'lib/web/main_web.dart'
      - 'lib/web/routs_web.dart'
      - 'lib/web/data/services_locators.dart'
      - 'lib/core/**'
      - '.github/workflows/deploy-admin.yml'
  workflow_dispatch:

  # نعيد البناء عشان نقرأ الـ repository secrets (SUPABASE_URL/ANON_KEY/MAPS)

permissions:
  contents: write

concurrency:
  group: pages-admin
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
      - name: Install dependencies
        run: flutter pub get
      - name: Debug secret presence
        run: |
          echo "SUPABASE_URL length: ${#SUPABASE_URL}"
          echo "SUPABASE_ANON_KEY length: ${#SUPABASE_ANON_KEY}"
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}

      - name: Build admin web
        run: flutter build web --target lib/web/main_web.dart --release --base-href /ship_link/admin/ --no-wasm-dry-run --dart-define=SUPABASE_URL="${{ secrets.SUPABASE_URL }}" --dart-define=SUPABASE_ANON_KEY="${{ secrets.SUPABASE_ANON_KEY }}" --dart-define=GOOGLE_MAPS_API_KEY="${{ secrets.GOOGLE_MAPS_API_KEY }}"
      # نضمن إن كل الـ links تتبع /ship_link/admin/ (نصلح الـ manifest reference)
      - name: Patch index.html base href
        run: |
          if ! grep -q '<base ' build/web/index.html; then
            sed -i 's|<head>|<head><base href="/ship_link/admin/">|' build/web/index.html
          fi
      # نكسر الـ cache بتاع الـ JS عشان المستخدم يشوف آخر نسخة فوراً
      # بنضيف query param (commit sha) على main.dart.js جوه flutter_bootstrap.js
      - name: Cache-bust JS with commit SHA
        run: |
          sed -i "s|main.dart.js|main.dart.js?v=${GITHUB_SHA}|g" build/web/flutter_bootstrap.js
          echo "Patched flutter_bootstrap.js with version ${GITHUB_SHA}"
      - name: Deploy to gh-pages under /admin (keep other files)
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build/web
          destination_dir: admin
          keep_files: true
