# Copy this file to `.env` and fill in real values.
# `.env` is gitignored — NEVER commit the real keys.
# Build with: flutter build apk --release --flavor user --target lib/user/main_user.dart --split-per-abi --dart-define-from-file=.env

SUPABASE_URL=REPLACE_ME
SUPABASE_ANON_KEY=REPLACE_ME
GOOGLE_MAPS_API_KEY=REPLACE_ME
FIREBASE_API_KEY=REPLACE_ME
FIREBASE_APP_ID=REPLACE_ME
FIREBASE_MESSAGING_SENDER_ID=REPLACE_ME
FIREBASE_PROJECT_ID=REPLACE_ME
FIREBASE_STORAGE_BUCKET=REPLACE_ME
